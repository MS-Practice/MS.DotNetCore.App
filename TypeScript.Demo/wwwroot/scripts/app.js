function sayHello() {
    var compiler = document.getElementById("compiler").value;
    var framwork = document.getElementById("framework").value;
    return "Hello from " + compiler + " and " + framwork + "!";
}
//# sourceMappingURL=app.js.map